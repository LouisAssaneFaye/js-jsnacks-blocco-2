const array[];
array.push(n1);
let i = 0;
let superato30=false;
while ( superato30===false) {
    const n = parseInt(prompt("Type a number"));
    array.push(n);
    }const somma += array[i];{
    if( somma > 50 ){
        superato30=true;
    }
    i++;
}