let array[];
let i = 0;
let superato50=false;
while ( superato50 === false) {
    const n = parseInt(prompt("Type a number"));
    array.push(n);
    }const somma += array[i];{
    if( somma > 50 ){
        superato50=true;
    }
    i++;
}